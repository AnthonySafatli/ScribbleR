import { Button } from "react-bootstrap";

function RoomIdSubmit() {
    return (
        <div>
            <Button variant="primary" type="submit" >
                Create Room
            </Button>
        </div>
    );
}

export default RoomIdSubmit;